framework:
    mailer:
        dsn: '%env(MAILER_DNS)%'
        envelope:
            sender: 'snakeater95@gmail.com'
            #            recipients: ['snakeater95@gmail.com']
        headers:
            from: 'RENTED <support@ac-malin.fr.nf>'

when@prod:
    framework:
        mailer:
            dsn: '%env(MAILER_DNS)%'
            envelope:
                sender: 'contact@rented.fr'
            headers:
                from: 'RENTED <contact@rented.fr>'
